apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: www
spec:
  parentRefs:
    - name: gateway
      namespace: istio-public
  hostnames:
    - www.stefanmuraru.local
    - stefanmuraru.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: www
          port: 80
